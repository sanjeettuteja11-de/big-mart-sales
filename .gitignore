# Competition data needs an Analytics Vidhya login and isn't ours to
# redistribute. Put the train/test CSVs in data/raw/ locally.
data/raw/*
!data/raw/.gitkeep

# Regenerated by src/train.py and src/tune.py.
outputs/oof/
outputs/test_preds/
submissions/**/*.csv
reports/figures/
# outputs/cv_results.json and outputs/params/*.json ARE committed: they are a
# few KB and are the evidence behind the numbers in the README.

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/
catboost_info/

# OS
.DS_Store
